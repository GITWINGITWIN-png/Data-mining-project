# PROGRESS — ElderCare Insight (Mini DW & Dashboard)

Group Assignment #1 — Data Mining / Data Warehouse

## สถานะปัจจุบัน

**ตอบครบทั้ง 8 คำถาม และแดชบอร์ดใช้งานได้แล้ว (2026-08-22)**
เหลือเฉพาะรายงาน PDF 10–15 หน้า และวิดีโอนำเสนอ 8–15 นาที

- คำตอบทั้ง 8 ข้อพร้อมตัวเลขและข้อจำกัด → `06_Report/BQ_answers.md`
- แดชบอร์ด → `04_Dashboard/app.py` (`streamlit run app.py`)
- ภาพหน้าจอสำหรับรายงาน → `04_Dashboard/screenshots/` (9 ภาพ)

## Milestones

| # | ขั้น | สถานะ |
|---|------|-------|
| 1 | สำรวจแหล่งข้อมูล + ยืนยันตัวเลขจริง | ✅ เสร็จ |
| 2 | เอกสารออกแบบ (Kimball 4 ขั้น, star schema, กฎคุณภาพ Q1–Q8) | ✅ เสร็จ |
| 3 | ปิดข้อสงสัยก่อนพัฒนา (หัวข้อ 20) | ✅ เสร็จ 2026-08-12 |
| 4 | ETL — Extract | ✅ เสร็จ |
| 5 | ETL — Clean / Transform / Integrate / Load | ✅ เสร็จ · บั๊กที่พบทั้ง 7 ข้อแก้แล้ว |
| 6 | คลังข้อมูล DuckDB (6 Dim + 2 Fact) | ✅ สร้างได้ · ผ่านตรวจ 22 ข้อ · รันซ้ำได้ผลเดิม |
| 7 | S4 ประชากร 65+ (`Ref_State_Population`) | ✅ เสร็จ 2026-08-12 |
| 8 | ตอบ BQ1 + กราฟ (notebook) | ✅ เสร็จ 2026-08-12 |
| 9 | ตอบ BQ2 + BQ8 (notebook) | ✅ เสร็จ 2026-08-12 |
| 10 | ตอบ BQ3–BQ7 + กราฟ | ✅ เสร็จ 2026-08-22 |
| 11 | Dashboard (Streamlit) | ✅ เสร็จ 2026-08-22 · ผ่านทดสอบ 11/11 กรณี |
| 12 | รายงาน PDF 10–15 หน้า | ⬜ ยังไม่เริ่ม |
| 13 | วิดีโอนำเสนอ 8–15 นาที | ⬜ ยังไม่เริ่ม |

## แดชบอร์ด — ตรวจครบตามข้อกำหนดข้อ 5 แล้ว

| ข้อกำหนด | ต้องมี | ที่ทำได้ |
|---|---|---|
| Measures สรุปผลประกอบการ | ≥ 3 | **8** (M1, M3, M4, M5, M6, M7, M8, M9) |
| กราฟ | ≥ 5 | **14 การแสดงผล** จากฟังก์ชันกราฟ 12 ตัว |
| วิเคราะห์ตามช่วงเวลา | ≥ 1 | **2** — ค่าปรับรายไตรมาส (BQ7) และ cohort 2562→2569 (BQ5) |
| เปรียบเทียบข้อมูล | ≥ 1 | **5** — เจ้าของ, ขนาดเครือ, ควอไทล์ชั่วโมง, ตารางไขว้ลาออก×ชั่วโมง, รายรัฐ |
| Filter / Interactive control | ≥ 2 | **10** — งวด, รัฐ, กลุ่มเจ้าของ, ขนาดเครือ, สวิตช์แถวสงสัย + ตัวควบคุมในแท็บอีก 5 |
| Insight และข้อเสนอแนะ | ≥ 5 | **8 ข้อเสนอแนะ** + insight ประกอบกราฟราว 15 จุด |

**คะแนนพิเศษที่เข้าเกณฑ์** — Streamlit (เครื่องมือสมัยใหม่) · drill-down รายรัฐและรายแห่ง ·
ตรวจคุณภาพข้อมูลอัตโนมัติที่อ่านจาก `etl_run_log` สด ๆ · ดึงข้อมูลผ่าน API (Census) ·
incremental load ในชั้น ETL · ดาวน์โหลดรายชื่อเฝ้าระวังเป็น CSV

## BQ3–BQ7 — ตอบแล้ว (2026-08-22)

รายละเอียดเต็มพร้อมตารางและข้อจำกัดอยู่ใน **`06_Report/BQ_answers.md`**
ด้านล่างเก็บเฉพาะข้อค้นพบที่กระทบการตัดสินใจ

### BQ3 — จุดคุ้มของชั่วโมงพยาบาลอยู่ที่ 3.48 ซึ่งตรงกับเกณฑ์ CMS พอดี

| | ต่ำกว่า 3.48 ชม. | ถึงเกณฑ์ขึ้นไป |
|---|---|---|
| สถานพยาบาล | 5,134 (**36.0%**) | 9,114 |
| ดาวตรวจสุขภาพ | 2.49 | 3.01 |
| ค่าปรับต่อเตียง | **$404** | $268 |
| % ที่เคยถูกปรับ | 52.6% | 39.7% |

**มีสองจุดคุ้ม ไม่ใช่จุดเดียว** — ต้องไปให้ถึง 3.48 (ใต้เส้นค่าปรับสูงกว่า 1.51 เท่า)
แต่ **เลิกคุ้มที่ราว 3.75** เพราะช่วง 3.48–4.25 เส้นค่าปรับแบนแล้ว ($289 → $264 → $283)
งบก้อนถัดไปจึงควรไปที่บ้านที่ยังอยู่ใต้เกณฑ์ ไม่ใช่เติมชั่วโมงให้บ้านที่ผ่านแล้ว

**กับดักที่ต้องระวังและปิดไปแล้ว** CMS เอาชั่วโมงพยาบาลไปคิดเป็น *ดาวด้านการจัดพยาบาล*
ซึ่งเป็นองค์ประกอบของคะแนนรวมอยู่แล้ว การพล็อตชั่วโมงกับคะแนนรวมจึงเป็นวงกลมบางส่วน
→ ข้อสรุปทั้งหมดยืนบน **ดาวการตรวจสุขภาพ** (ผู้ตรวจให้คะแนนหน้างาน ไม่มีชั่วโมงในสูตร)
และ **ค่าปรับ** แทน · คุมตัวแปรเจ้าของแล้วความชันยังอยู่ครบทั้งสามกลุ่ม

### BQ4 — มีสองเส้น และคนละคนใช้คนละเส้น

- **คุณภาพไม่มีระดับปลอดภัย** คะแนนดาวลดตั้งแต่ช่วงแรกสุด ทุก 5 จุดแลกราว 0.2 ดาว
- **อัตราการเข้าพักหักที่ 40%** ต่ำกว่านั้นทรงตัว ~83.5% แล้วเสีย 1.57 จุดทันทีในช่วง
  40–45% และพังที่ 65%+ เหลือ 71.7%
- **การลาออกไม่ใช่แค่เงาของการจัดพยาบาลบาง** — ในควอไทล์ที่จัดพยาบาลดีที่สุด
  การลาออกจาก <40% ไป 60%+ ยังทำคะแนนตก **1.34 ดาว** (4.15 → 2.81) ซึ่ง*มากกว่า*
  ช่วงห่างในควอไทล์ที่บางที่สุด → เป็นคานงัดอิสระ และถูกกว่าการจ้างเพิ่มถาวร

### BQ5 — ฟื้นเชิงพาณิชย์ ไม่ฟื้นเชิงคุณภาพ และการฟื้นมาจากอุปทานหดตัว

วัดจากสถานพยาบาลชุดเดียวกัน 14,231 แห่ง: เข้าพัก 79.8% → 80.2% (**+0.4 จุด**)
แต่คะแนนดาว 3.36 → 2.97 (**−0.39**) และข้อบกพร่อง 7.50 → 9.28 (**+23.7%**)

**ข้อค้นพบที่เปลี่ยนวิธีอ่านตัวเลข** มี **1,282 แห่ง (8.25%) ปิดตัว พาเตียงออกไป 98,725 เตียง**
และแห่งที่ปิดมีอัตราการเข้าพักเดิมเพียง **64.9%** เทียบ 79.8% ของแห่งที่รอด
ระดับประเทศเตียง −5.1% ขณะผู้พักอาศัย −3.5% → **ตัวหารออกจากตลาด อัตราส่วนจึงดูดีขึ้น**
ห้ามอ่านอัตราการเข้าพักที่ฟื้นเป็นสัญญาณว่าอุปสงค์กลับมา

### BQ6 — คะแนนเสี่ยง 5 องค์ประกอบ ผ่านการทดสอบย้อนหลัง

ให้คะแนนจากงวด ม.ค. 2562 → ดูผลจริงปี 2566–2569 (คะแนนไม่เคยเห็นผลลัพธ์ ห่างกัน 4 ปี)

| คะแนนเสี่ยง | % ถูกปรับ | ค่าปรับต่อเตียง | ดาวปี 2569 |
|---|---|---|---|
| 0 | 35.6% | $215 | 3.45 |
| 6 | **59.5%** | **$550** | **2.21** |

ขึ้นทุกขั้นในทุกคอลัมน์ผลลัพธ์ · กลุ่มเสี่ยงสูง 1.29× อัตราถูกปรับ และ 1.52× มูลค่าค่าปรับ
**ข้อสรุปที่สำคัญกว่าตัวคูณ: บ้านที่เสี่ยงสูงในปี 2562 ยังได้เพียง 2.21 ดาวหลังผ่านไป 7 ปี**
— ความเสี่ยงคงทน ซื้อกิจการที่ติดธงต้องตั้งงบพลิกฟื้น ไม่ใช่หวังว่าจะดีขึ้นเอง

### BQ7 — ต่างกัน 24 เท่าระหว่างรัฐ และรูปแบบการลงโทษเปลี่ยนไป

- **รายรัฐ** VT $1,364/เตียง สูงสุด · IN $57 ต่ำสุด · ประเทศ $324 · **24 เท่า**
  ภายใต้กฎกลางชุดเดียวกัน · IL คือกรณีที่ต้องระวังที่สุด (ใหญ่ที่สุด $81.7M *และ* เข้ม $960/เตียง)
- **กระจุกตัว** 5 รัฐ (IL, TX, CA, OH, FL) กินค่าปรับ **43.9%** บนเตียงเพียง **31.8%**
- **รายเวลา** ค่าปรับเฉลี่ยต่อครั้ง 2566 → 2568 **+76.7%** ($24,230 → $42,817)
  ขณะจำนวนครั้ง **−19.2%** → เปลี่ยนจาก "ปรับบ่อยทีละน้อย" เป็น "ปรับน้อยครั้งแต่หนัก"
- **กราฟต้องแสดงรูสองรู ไม่ใช่ซ่อน** ช่วง 2562–2565 ไม่มี snapshot ใดครอบคลุม (ตัดเส้น)
  และไตรมาสท้ายของแต่ละยุคถูกตัดด้วยความล่าช้าของการยื่น (วาดเป็นจุดกลวง ไม่นับในเส้น)

## BQ2 — ตอบแล้ว (2026-08-12)

**คำตอบ (งวด 2026-07-01 · ค่าปรับช่วง 2023-07-01 ถึง 2026-07-01)**

| | For profit | Non profit | Government |
|---|---|---|---|
| สถานพยาบาล | 10,845 | 2,875 | 947 |
| คะแนนดาวเฉลี่ย | **2.80** | 3.57 | 3.28 |
| ชม.พยาบาล/คน/วัน | **3.62** | 4.21 | 4.17 |
| สัดส่วน RN | **15.8%** | 19.4% | 18.3% |
| อัตราลาออก | **45.8%** | 40.4% | 42.4% |
| ค่าปรับต่อเตียง | **$351** | $224 | $250 |
| % ที่เคยถูกปรับ | **48.7%** | 37.1% | 43.7% |

**For-profit ต่ำกว่าในทุกมิติคุณภาพและเสี่ยงค่าปรับสูงกว่า** — ช่องว่างคะแนนดาว 0.77
ชั่วโมงพยาบาลน้อยกว่า 16% ค่าปรับต่อเตียงสูงกว่า 1.57 เท่า
Government มีอัตราการเข้าพักต่ำกว่ากลุ่มอื่นชัดเจน (72.8% vs ~80%) ซึ่งเป็นเรื่องพาณิชย์ ไม่ใช่คุณภาพ

**ทำไมต้องตอบที่ระดับ 3 กลุ่ม ไม่ใช่ 13 ประเภท** — ปี 2562→2569 สถานพยาบาลเปลี่ยน
*ประเภท* ราวครึ่งหนึ่ง แต่เปลี่ยน *กลุ่ม* ราว 10% และการเปลี่ยนที่พบบ่อยสุดคือ
`Corporation` → `Limited Liability company` (3,777 แห่ง) ซึ่งเป็นการเปลี่ยนรูปแบบนิติบุคคล
ไม่ใช่เปลี่ยนผู้บริหาร → **กฎ Q5 ที่ยุบ 13 ประเภทเหลือ 3 กลุ่ม มีเหตุผลเชิงประจักษ์รองรับแล้ว**

## BQ8 — ตอบแล้ว (2026-08-12)

**คำตอบ: ไม่ได้เปรียบด้านคุณภาพ — เป็นการกดต้นทุนแรงงาน**

| | อิสระ | เครือใหญ่ (50+) | ต่าง |
|---|---|---|---|
| คะแนนดาวเฉลี่ย | 3.24 | 2.92 | **−0.32** |
| ชม.พยาบาล/คน/วัน | 4.09 | 3.53 | **−13.8%** |
| สัดส่วน RN | 17.8% | 16.4% | −1.4 จุด |
| อัตราลาออก | 41.6% | 46.2% | **+4.6 จุด** |
| อัตราการเข้าพัก | 78.5% | 80.1% | +1.6 จุด |

**หลักฐานที่ปิดข้อโต้แย้งเรื่อง for-profit:** เครือใหญ่เกือบทั้งหมดเป็น for-profit
จึงต้องคุมตัวแปร — ผลคือ **ชั่วโมงพยาบาลลดลงทุกขั้นตามขนาดเครือ ในทั้งสามกลุ่มเจ้าของ**

| กลุ่ม | อิสระ → เล็ก → กลาง → ใหญ่ | ลดทุกขั้น |
|---|---|---|
| For profit | 3.75 → 3.67 → 3.61 → 3.54 | ✅ |
| Non profit | 4.38 → 4.13 → 4.03 → 3.47 | ✅ |
| Government | 4.56 → 4.08 → 3.42 → 3.34 | ✅ |

ขนาดเครือจึงมีผลที่รูปแบบการถือครองอธิบายไม่ได้
สิ่งที่เครือใหญ่ได้เปรียบจริงคือ**ด้านพาณิชย์** (เข้าพักสูงกว่า) ไม่ใช่ด้านการดูแล

**ข้อยกเว้นที่บันทึกไว้:** ในกลุ่ม for-profit เครือใหญ่สุดคะแนนดีกว่าเครือกลางเล็กน้อย
(2.91 vs 2.75) และค่าปรับต่อเตียงต่ำกว่า — เป็นไปได้ว่าใหญ่ถึงระดับหนึ่งมีระบบกำกับ
การปฏิบัติตามกฎที่ชดเชยการจัดพยาบาลที่บางลงได้บางส่วน แนวโน้มไม่ใช่เส้นตรง

**ข้อจำกัด:** ข้อมูลเครือมีเฉพาะปี 2569 (คอลัมน์เพิ่มเข้ามาปี 2568) จึงยังแยกไม่ได้ว่า
เครือทำให้บ้านแย่ลง หรือเครือไปซื้อบ้านที่แย่อยู่แล้ว · เครือของรัฐมีเพียง 91–173 แห่ง
(กราฟใช้จุดกลวงกำกับช่องที่มีน้อยกว่า 100 แห่ง) · ยังไม่ได้คุมตัวแปรรัฐ

## กับดักที่ปิดได้ในสองข้อนี้

**BQ2 ต้องใช้ทั้งสอง Fact ที่ Grain ต่างกัน** — ถ้า `JOIN` ตรง ๆ เตียงของกลุ่ม for-profit
จะกลายเป็น **2,586,212 แทนที่จะเป็น 1,203,047 คือพองขึ้น 2.1 เท่า** (ทดสอบไว้ในโน้ตบุ๊ก)
`queries.segment_compare()` จึงรวมค่าแต่ละ Fact แยกกันให้เหลือระดับกลุ่มก่อน
แล้วค่อยเชื่อมที่ระดับกลุ่ม

**การถ่วงน้ำหนักชั่วโมงพยาบาล** — HPRD เป็นอัตราต่อหัวอยู่แล้ว การเฉลี่ยแบบไม่ถ่วงน้ำหนัก
ให้บ้าน 20 เตียงมีน้ำหนักเท่าบ้าน 300 เตียง ฟังก์ชันจึงถ่วงด้วยจำนวนผู้พักอาศัย
และคืนค่าไม่ถ่วงน้ำหนักมาด้วยเพื่อให้เห็นช่องว่าง

## BQ1 — ตอบแล้ว (2026-08-12)

**คำตอบ (งวด 2026-07-01 · ประชากรปี 2024 · จัดอันดับ 51 รัฐ)**

| อันดับ | รัฐ | คะแนน | เตียง/พันคน 65+ | เข้าพัก | คู่แข่ง |
|---|---|---|---|---|---|
| 1 | New Mexico | 0.78 | 16.1 | 83.4% | 2.85 |
| 2 | Virginia | 0.75 | 21.1 | 87.8% | 2.96 |
| 3 | South Carolina | 0.74 | 18.1 | 87.2% | 3.00 |
| 4 | Maine | 0.71 | 17.7 | 87.3% | 3.08 |
| 5 | West Virginia | 0.71 | 27.5 | 89.8% | 2.94 |

ระดับชาติ: 25.6 เตียง/พันคน · เข้าพัก 80.2% · คะแนน 2.99 · ควรเลี่ยง IA, IN, AR

**ไฟล์**

| ไฟล์ | หน้าที่ |
|---|---|
| `02_ETL/population.py` | S4 — ACS API ถ้ามี `CENSUS_API_KEY` ไม่มีก็ใช้ไฟล์ประมาณการ (ไม่ต้องใช้คีย์) |
| `04_Dashboard/queries.py` | ตรรกะ query ทั้งหมด — notebook, แดชบอร์ด และเอกสารคำตอบใช้ร่วมกัน |
| `04_Dashboard/charts.py` | กราฟ matplotlib — ใช้ร่วมกันเช่นกัน |
| `04_Dashboard/app.py` | **แดชบอร์ด Streamlit** · `streamlit run app.py` |
| `04_Dashboard/test_dashboard.py` | ทดสอบแดชบอร์ด 11 กรณีตัวกรอง · `python test_dashboard.py` |
| `04_Dashboard/make_screenshots.py` | ถ่ายภาพหน้าจอ 9 ภาพสำหรับรายงาน (ต้องเปิด server ก่อน) |
| `04_Dashboard/screenshots/` | ภาพหน้าจอ 9 ภาพ รวมภาพที่ใช้ตัวกรองแล้ว |
| `04_Dashboard/BQ1_state_market.ipynb` | โน้ตบุ๊ก 27 เซลล์ รันผ่านทั้งเล่ม มี output ติดมาด้วย |
| `04_Dashboard/bq*.png` | กราฟ 17 รูปสำหรับใส่รายงาน (BQ1–BQ8) |
| `04_Dashboard/requirements.txt` | pandas, duckdb, matplotlib, streamlit |
| `06_Report/BQ_answers.md` | **คำตอบครบทั้ง 8 ข้อ** พร้อมตาราง ข้อจำกัด และคำสั่งรันซ้ำ |

**การตัดสินใจเชิงออกแบบที่ควรรู้**

- **`pop_65plus` ไม่ได้อยู่ใน `Dim_Geography`** — dimension นั้นอยู่ที่ระดับ (zip, city, state)
  แต่ประชากรเป็นข้อมูลระดับรัฐ ถ้าใส่ลงทุกแถว ZIP แล้วมีใคร `SUM` จะได้ประชากรเกินจริง
  หลายสิบเท่า จึงแยกเป็น `Ref_State_Population` ที่ระดับ (รัฐ, ปี)
  → **เอกสารออกแบบยังเขียนว่า `pop_65plus` อยู่ใน Dim_Geography ต้องแก้**
- ตรรกะอยู่ในโมดูล ไม่ได้อยู่ในเซลล์ notebook เพื่อให้ Streamlit ใช้ตัวเดียวกันได้
  โดยไม่ต้องเขียนใหม่ ตัวเลขที่ตรวจในโน้ตบุ๊กจึงเป็นตัวเลขเดียวกับที่แอปจะวาด
- น้ำหนักเท่ากันทั้งสามตัวชี้วัดเป็น **ข้อสมมติ** เก็บ `pct_supply`/`pct_demand`/`pct_contest`
  ไว้ในผลลัพธ์ให้ปรับน้ำหนักเองได้

**ข้อจำกัดที่ต้องเขียนกำกับในรายงาน**

- ประชากรเป็นปี 2024 แต่ข้อมูลสถานพยาบาลเป็นปี 2026 → เตียงต่อพันคนสูงกว่าความจริง
  เล็กน้อย**ทุกรัฐเท่ากัน** จึงไม่กระทบอันดับ
- ตอบได้แค่ระดับรัฐ เพราะประชากรมีแค่ระดับรัฐ
- คะแนนดาวคือคุณภาพที่ CMS วัด ไม่ใช่ความสามารถทำกำไร — รัฐที่คู่แข่งคะแนนต่ำ
  อาจเป็นเพราะ Medicaid จ่ายต่ำจนไม่มีใครทำกำไรได้ ต้องดู BQ2/BQ3 ประกอบ
- GU และ PR ถูกตัดจากการจัดอันดับ (ไฟล์ประชากรระดับรัฐไม่ครอบคลุมดินแดน) **ติดธงไว้ ไม่ได้ลบ**

## สถานะคลังข้อมูลปัจจุบัน (4 งวด)

| ตาราง | แถว |
|---|---|
| Dim_Date | 24,838 |
| Dim_Facility (SCD2) | 25,937 |
| Dim_Geography | 9,729 |
| Dim_Ownership | 14 |
| Dim_Chain | 652 |
| Dim_Penalty_Type | 3 |
| Fact_Facility_Monthly | 59,665 |
| Fact_Penalty_Event | 27,529 |

ยอดค่าปรับรวม **$785,612,160** · กระทบยอดกับคอลัมน์ที่ CMS คำนวณเอง ตรงกัน 6,563/6,563 แห่ง
· รันสองรอบได้ SHA-256 เหมือนกันทั้ง 8 ตาราง

## ผลตรวจ ETL (2026-08-12)

รันจริงด้วย 4 งวด (2019-01-17, 2026-04-29, 2026-06-24, 2026-08-06) แล้วตรวจผลลัพธ์

**ต้องแก้ก่อนใช้ตัวเลขค่าปรับ**

1. ~~**Fact_Penalty_Event นับซ้ำ ~382 ล้านดอลลาร์**~~ → ✅ **แก้แล้ว 12 ส.ค. 2569**
   เปลี่ยนตัวระบุตัวตนเป็นคีย์ธรรมชาติ + `_occurrence` **เสมอทุกยุค** (`facts._natural_key`)
   และเติม `fine_id` ย้อนกลับให้เหตุการณ์ที่เกิดก่อนงวด 202606 เพื่อรักษารหัสของ CMS
   ไว้เป็นแอตทริบิวต์ พร้อมเพิ่ม `facts._audit_key_against_fine_id` ที่ใช้ Fine ID
   *ตรวจทาน* คีย์ ไม่ใช่สร้างคีย์
   - เหตุการณ์ 39,459 → **27,529** · ยอดค่าปรับ $1,165,130,372 → **$785,612,160**
   - `key_audit` ได้ **13,710 คีย์ ต่อ 13,710 Fine ID แบบ one-to-one พอดี** (0 ยุบเกิน 0 แตกเกิน)
   - ค่าปรับ $5,782 สามใบของ CCN 015060 วันเดียวกัน **ยังอยู่ครบ** พร้อม Fine ID แยกกัน
   - รันซ้ำได้ผลเหมือนเดิม (SHA ตรงกัน) และ **ไม่ขึ้นกับลำดับที่ระบุงวด**
   - เหลือ 2 เหตุการณ์ ($27,498 = 0.0035%) ที่ CMS เองรายงาน 2 แถวในงวด เม.ย.
     แล้วเหลือ 1 แถวในงวด มิ.ย. → **ติดธงไว้ ไม่ลบ** ตามหลักของโปรเจกต์
   - `verify_facts.py` เพิ่ม invariant ที่จับบั๊กคลาสนี้ได้โดยไม่ต้องพึ่ง Fine ID:
     กลุ่มที่เหมือนกันต้องไม่มีแถวมากกว่าที่งวดใดงวดหนึ่งรายงาน — และถอด
     `n_periods == 3` ที่ฝังตายออก เปลี่ยนเป็นอนุมานจากข้อมูล
   - **ผ่าน 22 ข้อ ตก 0 ข้อ**

1a. ~~เดิม~~ (เก็บไว้เป็นบันทึก) — `facts._dedup_key`
   ให้คีย์ต่างกันสองแบบกับค่าปรับ *รายการเดียวกัน* ขึ้นกับว่าเห็นครั้งแรกในงวดที่มี
   Fine ID หรือไม่ (`FID:38063` กับ `NAT:015019|...`) ทั้งสองแถวจึงรอดจาก dedup
   วัดได้ 11,673 กลุ่ม 12,189 แถวเกิน — โผล่เฉพาะเมื่อมีงวดคาบเกี่ยว 202606
   ซึ่งชุดทดสอบเดิม (2019 + Jun + Jul) ไม่มี
   **ทางแก้:** ใช้คีย์ธรรมชาติ + `_occurrence` เป็นตัวระบุตัวตน *เสมอทุกยุค*
   (คีย์นี้ไม่ขึ้นกับยุคและเก็บกรณีวันเดียวจำนวนเงินเท่ากันได้ครบ)
   แล้วลด `fine_id` ลงเป็นแอตทริบิวต์สำหรับ *ตรวจทาน* ไม่ใช่ตัวระบุตัวตน
2. **`verify_facts.py` รายงาน 21/21 ผ่าน บนข้อมูลที่เสีย** — จุดบอดสามจุด:
   เช็ค `no Fine ID appears twice` มองไม่เห็น (แถวซ้ำมี fine_id เป็น NULL);
   การกระทบยอดข้อ 4 กรองด้วย `jun_fine_ids` ซึ่งตัดแถวซ้ำออกโดยโครงสร้าง
   แล้วไปโผล่เป็นหมายเหตุ "reported after the Jun file was published" $433.8M
   ซึ่ง $379.5M (87%) คือการนับซ้ำ; และไม่มีเช็คยอดเงินรวมเลย
3. **`n_periods == 3` ฝังตายในเทสต์** — ผ่านแบบไม่ได้ทดสอบอะไร (ตอนผ่านยังไม่ได้
   โหลดงวด 2026-08-06 ที่ชื่อเทสต์อ้างถึง) และ **ล้มทันที** เมื่อโหลดงวดนั้นเข้าไป

1b. ~~**`geography_key` แตกเพราะตัวพิมพ์ของชื่อเมือง**~~ → ✅ **แก้แล้ว 12 ส.ค. 2569**
   สร้าง `clean.geography_key_columns()` เป็นตัว normalize คีย์ธรรมชาติของ Dim_Geography
   **ตัวเดียวที่ทั้งสองฝั่งเรียกร่วมกัน** (`dimensions.build_dim_geography` และ
   `facts._geography_key`) เพราะถ้าแก้ฝั่งเดียว join จะพังทั้งหมดแบบเงียบ ๆ
   — ทุกแถวกลายเป็น Unknown โดยไม่มี error
   - Dim_Geography 12,109 → **9,729 แถว** (แถวซ้ำจากตัวพิมพ์เหลือ **0**)
   - สถานพยาบาลที่ถูกผ่าเพราะตัวพิมพ์ **3,310 → 0** · ที่ย้ายจริง 225 **ยังอยู่ครบ**
   - CCN 045140 ZIP 72143 AR ใช้ `geography_key 259` ตลอดทั้งสี่งวด (เดิม 267 → 268)
   - ZIP 84405 จาก 5 แถว เหลือ 3 แถวที่เป็นเทศบาลต่างกันจริง
     (OGDEN / SOUTH OGDEN / WASHINGTON TERRACE)
   - FK **0 orphans 0 Unknown** · ผ่าน 22 ข้อ · รันซ้ำ SHA ตรงกันทั้ง 3 ตาราง
   - ผลพลอยได้: พิกัด lat/long ถูกเฉลี่ยจากกลุ่มที่ถูกต้องแล้ว (9,147 แถวมีพิกัด)
   - **ผลข้างเคียงเดียว:** `city` เก็บเป็นตัวพิมพ์ใหญ่ (`HUNTSVILLE`) ถ้าอยากให้แดชบอร์ด
     แสดงสวยกว่านี้ ใช้ `INITCAP(city)` ตอน query หรือเพิ่มคอลัมน์ display แยก

1c. ~~เดิม~~ (เก็บไว้เป็นบันทึก) — อาการที่พบ
   `dimensions.build_dim_geography` เรียก `normalize_text(df["city"])` **โดยไม่ใส่ `upper=True`**
   ขณะที่ `state_code`, `provider_name`, `legal_business_name`, `chain_name` ใส่ครบ
   สถานที่จริงแห่งเดียวจึงถูกแยกเป็นหลายแถว: Dim_Geography 12,108 แถว เหลือ **9,728**
   ถ้า `UPPER(city)` คือ **2,380 แถวเกินมาจากตัวพิมพ์ล้วน**
   ตัวอย่าง CCN 045140 ZIP 72143 AR: `SEARCY` → `Searcy` ทำให้ key 267 → 268
   **ที่เดียวกัน** และการพลิกเกิดระหว่าง เม.ย. กับ มิ.ย. 2569 คือ CMS เปลี่ยนเองเรื่อย ๆ
   ไม่ใช่ artifact ของรอยต่อยุค
   **ผลกระทบ:** ทุกการวิเคราะห์แนวโน้มที่ระดับเมืองหรือ ZIP จะถูกผ่าเป็นสองท่อนที่ไม่ต่อกัน
   **ทางแก้:** ใส่ `upper=True` ให้ `city` (และ `county_parish`) เหมือนที่ทำกับชื่อสถานพยาบาลแล้ว

**ที่เหลืออีก 5 ข้อ — แก้ครบแล้วทั้งหมด 12 ส.ค. 2569**

3. ✅ **เวอร์ชัน SCD2 อายุเป็นศูนย์** — ย้าย `_drop_republished_periods` จาก `facts.py`
   ไปเป็น `sources.drop_republished_periods()` แล้วเรียกใน `read_all()` **ก่อนที่ driver
   ตัวไหนจะเห็น frame** ทั้ง dims และ facts จึงได้ชุดงวดเดียวกัน (ตรงตามเจตนาที่
   `sources.py` เขียนไว้ในหัวไฟล์อยู่แล้ว)
   ทดสอบด้วยคู่งวดที่มี Processing Date เดียวกัน: เวอร์ชันอายุศูนย์ **1 → 0**
   และเวอร์ชันรวมลดจาก 25,937 → 25,936 (เวอร์ชันปลอมหายไปด้วย)
4. ✅ **`parse_date` ตัดสินรูปแบบทีละคอลัมน์** — ให้คะแนนทุกรูปแบบกับคอลัมน์ทั้งคอลัมน์
   แล้วเลือกตัวที่อธิบายค่าได้มากที่สุด ค่าที่ตัวชนะอ่านไม่ได้ปล่อยเป็น null และนับไว้
   ไม่ส่งต่อไปให้ปฏิทินอีกแบบเงียบ ๆ
   - คอลัมน์ D/M/Y จริง: **4/4 ถูก** (เดิม 2/4 ผิดโดยไม่มี log)
   - ถ้ามีสองรูปแบบอธิบายได้เท่ากันแต่ให้ผลต่างกัน (ทุกวันที่ ≤ 12) → **เตือนว่ากำกวม**
     ว่าต้องให้คนตัดสิน ไม่ปล่อยให้ลำดับใน list เลือกเอง
   - ทุกงวดบันทึกไว้ว่าเลือกรูปแบบใด: `format %Y-%m-%d chosen (15,578 of 15,578 parsed)`
5. ✅ **`FILE_PATTERNS` ครอบไฟล์ปี 2562 แล้ว** — `State(?:US)?Averages` ทำให้ "US"
   เป็น optional และ VBP รับทั้งเว้นวรรคและ underscore พร้อมทั้งไม่มี prefix `FY_`
   โดยยังบังคับคำว่า "Facility" ไว้ จึงไม่ไปชน `FY_YYYY_SNF_VBP_Aggregate_Performance.csv`
   ตรวจแล้วทั้งสอง ZIP เข้าครบ ไม่มีการชนกัน (2562 ไม่มีไฟล์ citations จริง ๆ)
6. ✅ **บูลีนเป็น BOOLEAN แล้ว** — ประกาศ `BOOLEAN_TRACKED` แล้วให้แถว Unknown
   ถือ `pd.NA` แทนสตริง พร้อม cast เป็น `"boolean"`
   DuckDB: `resides_in_hospital`/`ccrc_flag`/`abuse_icon` เป็น **BOOLEAN**
   ตัวกรอง `WHERE abuse_icon` ใช้ได้แล้ว (2,006 แถว)
7. ✅ **ไม่เขียน DB ถ้า validate ไม่ผ่าน** — แยก `load.write_run_log()` ออกจาก
   `load.write_tables()` ทั้งสอง driver จึงเก็บ log ได้ทุกกรณี แต่เขียนตารางเฉพาะเมื่อผ่าน
   ทดสอบด้วยการฉีดปัญหาปลอม: **exit 2 · DB ไม่เปลี่ยน (SHA เดิม) · log ยังถูกบันทึก**

**เพิ่มเติมที่แก้ไปด้วย**

- `_bed_size_band` คืน `pd.NA` แทนสตริง `"Unknown"` → ตรรกะ "ไม่รู้ → รู้" ทำงานได้จริง
- `normalize_special_focus` เติม `"None"` **เฉพาะเมื่อยุคนั้นมีคอลัมน์จริง** ถ้าไม่มีคอลัมน์
  ปล่อย null ไว้ เพื่อไม่ให้อ้างว่ารู้ว่าสถานพยาบาลไม่มีสถานะพิเศษ ทั้งที่ไม่มีใครบอก
- `Fact_Facility_Monthly` กัน CCN ซ้ำในงวดเดียวแล้ว (เดิมพึ่งความสะอาดของ source)
- ลบ `dimensions._differs()` ที่เป็น dead code และตรรกะขัดกับที่ `build_dim_facility` ใช้จริง

**ไม่ใช่บั๊ก — เป็นข้อสังเกตที่ต้องเขียนกำกับ**

- `resident_days` (M2) **ถูกต้องเมื่อใช้เป็นตัวถ่วงน้ำหนักภายในงวด** ตามที่โค้ดตั้งใจ
  แต่ `SUM` ข้ามงวดไม่มีความหมาย เพราะแต่ละงวดใส่วันของ *เดือนเดียว* ขณะที่งวดห่างกัน
  3 เดือน — ต้องเขียนกำกับไว้ในเอกสาร ไม่ใช่บั๊กที่ต้องแก้โค้ด
- `special_focus_status` ปี 2562 เป็น binary (Y/N) ปี 2569 มี 3 ระดับ (`SFF Candidate`
  เพิ่มมา) มี 430 เวอร์ชันเกิดที่รอยต่อยุค — แยกไม่ได้ว่าเป็น artifact ของโดเมน
  หรือการเปลี่ยนสถานะจริงในรอบ 7 ปี จึงไม่จัดเป็นข้อผิดพลาด

**ยังไม่ได้ทำ (ไม่ใช่บั๊ก แต่กระทบขอบเขตงาน)**

- `survey_summary`, `vbp`, `state_averages`, `citations` แกะออกมาแต่ไม่มีโมดูลไหนอ่าน
  → S3 (ตัวคูณเงินจูงใจ) ยังไม่เข้าคลัง **BQ2/BQ3 จึงยังตอบไม่ได้**
- `pop_65plus` ยังเป็น null ทั้งคอลัมน์ → **BQ1 กับ M10 ยังตอบไม่ได้**
- ปลายทาง C ของ API นิยามไว้ใน `config.py` แต่ไม่มีโค้ดเรียก → การตรวจหลัง Load
  ด้วยข้อมูลปัจจุบันตามที่เอกสารออกแบบวางไว้ยังไม่ได้ทำ

**ส่วนที่ตรวจแล้วถูกต้อง ไม่ต้องแก้**

- mapping คอลัมน์ทั้ง 78 รายการตรงกับไฟล์จริงทุกตัว ทั้งยุค 2562 และ 2569
- ประเภทการถือครองครบ 13 ค่า จับกลุ่มได้หมด ไม่มีค่าตกหล่น
- SCD2 ถูกต้องจริง 0 ปัญหาเชิงโครงสร้าง การเปลี่ยนชื่อเป็นการเปลี่ยนจริง
  (COOSA VALLEY NURSING FACILITY → COOSA VALLEY HEALTHCARE CENTER)
  ไม่มีการระเบิดเวอร์ชันปลอมที่รอยต่อยุค — ตรรกะ "ไม่รู้ → รู้" ทำงานถูก
- กระทบยอดเส้นทาง fine_id กับคอลัมน์ของ CMS ตรงกันถึงระดับดอลลาร์ 6,563/6,563 แห่ง
- เส้นทางงวดที่ไม่มีไฟล์ Penalties ทำงานถูก (ทดสอบด้วยงวด 2026-08-06 แล้ว)
- FK integrity 0 orphans ทุกคู่ และการข้ามไฟล์ที่โหลดแล้วทำงานถูก

## ผลการตรวจข้อสงสัย 4 ข้อ (2026-08-12)

ทั้งสี่ข้อในหัวข้อ 20 ของเอกสารออกแบบ ตรวจด้วยข้อมูลจริงแล้ว สรุปผลอยู่ในเอกสาร
รายละเอียดที่กระทบการเขียนโค้ด:

1. **ยอดค่าปรับใน ProviderInfo = หน้าต่างหมุน 3 ปี** ไม่ใช่ยอดสะสม
   ตรงกับผลรวมจากแฟ้ม Penalties ครบ 6,563 แห่ง (100%)
   → ค่าปรับ "ลดลง" ระหว่างงวดได้เอง ห้ามตีความว่าข้อมูลพัง

2. **โครงสร้างคอลัมน์เปลี่ยนทั้งระบบ** ปี 2562 มี 78 คอลัมน์ ชื่อแบบ `PROVNUM`
   ปี 2569 มี 99 คอลัมน์ ชื่อแบบ `CMS Certification Number (CCN)`
   → ต้องมีตารางจับคู่ชื่อคอลัมน์แยกตามยุค

3. **Census API ต้องใช้ key** (302 → `missing_key.html`, body ว่าง ไม่มี error)
   → ขอคีย์ฟรี หรือใช้ทางถอย `sc-est2024-agesex-civ.csv` ที่ทดสอบแล้วว่าใช้ได้

4. **`Fine ID` มีเฉพาะงวด 202606 ขึ้นไป** และว่างทั้งหมดบน Payment Denial
   → Fact 2 ต้องกำจัดซ้ำสองระบบ ถ้าใช้คีย์ธรรมชาติแทนจะกลืนค่าปรับหาย 2.09%

## ข้อค้นพบเพิ่มเติมที่ ETL ต้องรับมือ

- **ชื่อไฟล์มี 3 แบบ** — `*_Download.csv` (2562), `NH_*_Jun2026.csv` (ปกติ),
  `4pq5-n9py_2026-07-29_NH_*.csv` (งวดบางส่วน) — `glob` ธรรมดาพังแน่นอน
- **snapshot บางส่วนมีจริง** — งวด 2026-08-06 มีแค่ 4 ไฟล์ ไม่มี Penalties เลย
  ต้องตรวจรายชื่อไฟล์ก่อนประมวลผลทุกงวด และมี `manifest.json` ให้อ่านแทนการเดาชื่อ
- **แถวทดสอบผูกกับงวด** — CCN 015009 ได้ 90.53% จาก API (งวด ก.ค.)
  แต่ 91.93% จาก CSV งวด มิ.ย. — เทสต์ต้องระบุงวดกำกับเสมอ

## ที่ยังเหลือ

- [ ] **เติมหมายเลขกลุ่มและชื่อสมาชิก 3 คน** ลงหน้าปกเอกสารออกแบบ และช่องที่เว้นไว้
      ใน `05_AI_Usage_Log/AI_Usage_Log.md` (ห้ามให้ AI เดาชื่อ)
- [ ] **รายงาน PDF 10–15 หน้า** — วัตถุดิบพร้อมแล้วทั้งหมด: `06_Report/BQ_answers.md`
      (คำตอบ) · `04_Dashboard/screenshots/` (ภาพแดชบอร์ด + ภาพที่ใช้ตัวกรอง) ·
      `eldercare_dw_design.tex` (star schema, grain, กฎคุณภาพ)
- [ ] **วิดีโอ 8–15 นาที** — สมาชิกทุกคนต้องออกกล้องและพูด แบ่งช่วง 2/5/5 นาทีตามโจทย์
- [ ] อัปโหลดโฟลเดอร์ขึ้น Google Drive แล้วเปิดสิทธิ์ "Anyone with the link can view"
- [ ] เพิ่มลิงก์ Drive และลิงก์แดชบอร์ดลงในรายงาน
- [x] ~~ขอ Census API key~~ — ใช้ทางถอย `sc-est2024-agesex-civ.csv` แล้ว ได้ผลครบ 51 รัฐ

**ข้อจำกัดที่ยังปิดไม่ได้ และตัดสินใจแล้วว่าจะประกาศแทนการแก้** — ไม่มี snapshot
ช่วงปี 2563–2565 จึงยืนยัน Q6 (รูปแบบวันที่) และ Q8 (หน่วยอัตราลาออก) ได้แค่สองปลาย
ทุกข้อสรุปที่กระทบเรื่องนี้ (BQ4, BQ5, BQ7) ระบุข้อจำกัดกำกับไว้ในเอกสารคำตอบและบนแดชบอร์ดแล้ว

## หมายเหตุการ build

```bash
cd Group01_ElderCareDW/06_Report
latexmk -xelatex -outdir=build eldercare_dw_design.tex
cp build/eldercare_dw_design.pdf .
```

ต้องใช้ **XeLaTeX** เท่านั้น — เอกสารใช้ `fontspec` และ `\XeTeXlinebreaklocale "th"`
สำหรับตัดคำไทย pdfLaTeX คอมไพล์ไม่ผ่าน

**การรัน** โปรเจกต์ใช้ pip + `requirements.txt` (มีสองไฟล์: `02_ETL/` และ `04_Dashboard/`)
ระหว่างพัฒนาใช้ `uv run --with duckdb --with pandas --with matplotlib --with streamlit`
ได้เช่นกันโดยไม่ต้องสร้าง venv

```bash
cd Group01_ElderCareDW/04_Dashboard
python -m pip install -r requirements.txt
streamlit run app.py          # แดชบอร์ด
python test_dashboard.py      # ทดสอบ 11 กรณี
```
